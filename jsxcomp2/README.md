JSX & Components 2
==================

To run:

```
npm run build
npm run bundle
```

then run a server on the `src` directory, for example:

```
python3 -m http.server --directory src
```

and open `jsx2.html`.
